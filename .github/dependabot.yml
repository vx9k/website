# Weekly dependency updates, opened as PRs. Each PR gets a Cloudflare
# preview, so check it before merging.
version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
